延用linux的文件权限：
r 4 w 2 x 1 分别表示可读，可写，可执行

比如 drwxr-x--x ：

d 表示文件类型 目录
第一个rwx表示创建它的用户可读可写可执行
第二个r-x表示同用户组的用户可读不可写可执行
第三个--x表示其他用户不可读不可写可执行

对于root过的Android系统通过执行shell命令 chmod -R 777 可以将该文件及它的子文件全部修改成对所有用户可读可写可执行

对于系统级应用，他们的userId相同，所以文件权限对它们开放，可以共享数据
